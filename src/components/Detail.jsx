import React from 'react'

export const Detail = (props) => {
    return (
        <div>
            <div>Detail</div>
            <button onClick={props.tambahkeranjang}>Tambah Kerajang</button>
        </div>
    )
}
